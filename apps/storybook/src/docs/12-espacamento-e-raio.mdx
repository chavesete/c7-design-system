import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Fundações/Espaçamento e raio" />

# Espaçamento e raio

Padrões de layout usando Tailwind + tokens de raio.

## Espaçamento

Para espaçamentos, use a escala do Tailwind (`p-*`, `m-*`, `gap-*`, `space-y-*`).

Recomendações práticas:

- Layouts: `gap-4` / `gap-6`
- Cards: `p-4` (densidade padrão), `p-6` (conteúdo rico)
- Listas: `space-y-2` / `space-y-3`

<div className="grid grid-cols-1 gap-4 lg:grid-cols-3">
  <div className="rounded-lg border border-c7-border bg-c7-card p-4">
    <div className="text-sm font-semibold">Card (p-4)</div>
    <div className="mt-3 space-y-2">
      <div className="h-3 rounded bg-c7-muted" />
      <div className="h-3 rounded bg-c7-muted" />
      <div className="h-3 w-2/3 rounded bg-c7-muted" />
    </div>
  </div>

  <div className="rounded-lg border border-c7-border bg-c7-card p-6">
    <div className="text-sm font-semibold">Card (p-6)</div>
    <div className="mt-3 space-y-3">
      <div className="h-3 rounded bg-c7-muted" />
      <div className="h-3 rounded bg-c7-muted" />
      <div className="h-3 w-2/3 rounded bg-c7-muted" />
    </div>
  </div>

  <div className="rounded-lg border border-c7-border bg-c7-card p-4">
    <div className="text-sm font-semibold">Row (gap-4)</div>
    <div className="mt-3 flex items-center gap-4">
      <div className="h-10 w-10 rounded bg-c7-muted" />
      <div className="h-10 flex-1 rounded bg-c7-muted" />
      <div className="h-10 w-20 rounded bg-c7-muted" />
    </div>
  </div>
</div>

## Raio (border-radius)

O DS expõe tokens de raio via CSS variables (web):

- `--c7-radius`
- `--c7-radius-card`
- `--c7-radius-sheet`

<div className="mt-4 grid grid-cols-1 gap-4 sm:grid-cols-3">
  <div className="rounded border border-c7-border bg-c7-card p-4">
    <div className="text-sm font-semibold">radius</div>
    <div className="mt-3 h-14 border border-c7-border" style={{ borderRadius: "var(--c7-radius)" }} />
    <div className="mt-2 text-xs text-c7-muted-foreground">var(--c7-radius)</div>
  </div>

  <div className="rounded border border-c7-border bg-c7-card p-4">
    <div className="text-sm font-semibold">radius-card</div>
    <div className="mt-3 h-14 border border-c7-border" style={{ borderRadius: "var(--c7-radius-card)" }} />
    <div className="mt-2 text-xs text-c7-muted-foreground">var(--c7-radius-card)</div>
  </div>

  <div className="rounded border border-c7-border bg-c7-card p-4">
    <div className="text-sm font-semibold">radius-sheet</div>
    <div className="mt-3 h-14 border border-c7-border" style={{ borderRadius: "var(--c7-radius-sheet)" }} />
    <div className="mt-2 text-xs text-c7-muted-foreground">var(--c7-radius-sheet)</div>
  </div>
</div>
